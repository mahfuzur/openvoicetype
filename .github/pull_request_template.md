## What and why

<!-- What changes for someone dictating? Link the issue if there is one. -->

## Checks

- [ ] `shellcheck scripts/*.sh` passes
- [ ] `./scripts/build-app.sh` builds with no new warnings
- [ ] `./scripts/dictate.sh selftest` works
- [ ] Prompt or `post_process()` change: eval numbers before and after (`evals/run.py`, and `--runs 3`)
- [ ] UI change: snapshots from `--overlay-snapshots` or `--settings-snapshots`, attached below
- [ ] Docs updated (README, CLAUDE.md, CHANGELOG) if behaviour changed

## Screenshots or eval output
