Style: code editor or terminal (the text may be a code comment, a commit message, or a prompt to an AI coding assistant). Be exact with technical terms: file names (main.py), identifiers (process_order, userId), commands (brew install sox), flags (--verbose), paths and versions. Apply spoken symbols (underscore, dot, slash, dash) wherever they are part of a name. Use standard technical casing (JavaScript, PostgreSQL, API, JSON). Don't add backticks or Markdown other than list lines.
